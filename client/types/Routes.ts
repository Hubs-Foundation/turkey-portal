export enum LoggedOutComponentPages {
  Login = 'login',
  Subscribe = 'subscribe',
}

export enum RoutesE {
  Login = '/login',
  Dashboard = '/dashboard',
  Hubs = '/hubs',
  Subscribe = '/subscribe'
}
