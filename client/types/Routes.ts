export enum LoggedOutComponentPages {}

export enum RoutesE {
  DASHBOARD = '/dashboard',
  HUBS = '/hubs/[hub_id]',
  SUBSCRIBE = '/subscribe',
  CONFIRM_PLAN = '/confirm-plan',
  CUSTOM_CLIENT = '/custom-client',
}
